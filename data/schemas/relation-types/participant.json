{
  "$schema": "../../../packages/schema-engine/meta-schemas/relation-type.schema.json",
  "id": "participant",
  "schema_version": 1,
  "labels": {
    "en": { "active": "Participant", "inverse": "Participated in" },
    "fr": { "active": "Participant", "inverse": "A participé à" }
  },
  "valid_from_types": ["event"],
  "valid_to_types": ["character", "crew"],
  "qualifiers": [
    {
      "id": "role",
      "value_type": "enum",
      "enum_ref": "event-roles",
      "required": false
    },
    {
      "id": "side",
      "value_type": "enum",
      "enum_ref": "event-sides",
      "required": false
    },
    {
      "id": "outcome",
      "value_type": "enum",
      "enum_ref": "event-outcomes",
      "required": false
    }
  ],
  "allow_multiple_concurrent": true,
  "inverse_inferred": true
}
